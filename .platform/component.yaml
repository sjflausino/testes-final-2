apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: "testes-final-2"
  description: "Artifacts generator via Spec."
  annotations:
    github.com/project-slug: sjflausino/testes-final-2
    backstage.io/techdocs-ref: dir:..
    github.com/workflows: publish.yml,remove.yml
    kong-manager/service-name: app.testes-final-2.80
    kong-manager/instance: cluster-vkdr-local
spec:
  type: devops
  lifecycle: experimental
  owner: "group:default/admin"
  providesApis:
    - testes-final-2
